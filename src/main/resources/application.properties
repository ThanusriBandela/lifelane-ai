# ─────────────────────────────────────────
# LOCAL DEV  (uses H2 in-memory)
# ─────────────────────────────────────────
spring.datasource.url=${DATABASE_URL:jdbc:h2:mem:lifelane}
spring.datasource.driverClassName=${DB_DRIVER:org.h2.Driver}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.H2Dialect}

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ─────────────────────────────────────────
# PRODUCTION (Render injects DATABASE_URL)
# Set DB_DRIVER=org.postgresql.Driver
# Set DB_DIALECT=org.hibernate.dialect.PostgreSQLDialect
# ─────────────────────────────────────────

# Groq AI API Key
groq.api.key=${GROQ_API_KEY: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}

# Fast2SMS API Key
fast2sms.api.key=${FAST2SMS_API_KEY:YOUR_FAST2SMS_API_KEY}

# ─────────────────────────────────────────
# GMAIL SMTP — for crash alert emails
#
# STEP-BY-STEP SETUP (READ CAREFULLY):
#
# 1. Go to https://myaccount.google.com
# 2. Security → Enable 2-Step Verification (mandatory)
# 3. After enabling, search "App passwords" in the search bar
# 4. Select "Mail" and "Windows Computer" (or any device)
# 5. Click "Generate" — you get a 16-character password like: abcd efgh ijkl mnop
# 6. Remove the spaces → use "abcdefghijklmnop" as your GMAIL_APP_PASSWORD
# 7. Set your Gmail address as GMAIL_ADDRESS
#
# COMMON MISTAKES THAT CAUSE "no emails received":
#  ✗ Using your regular Gmail password (does NOT work, must be App Password)
#  ✗ 2-Step Verification is OFF (App passwords won't appear in Google account)
#  ✗ "Less secure app access" setting (deprecated — use App Password instead)
#  ✗ Typo in the from address or contact email
# ─────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lifelane2026@gmail.com
spring.mail.password=zaep mdvp hhkt bsms
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

server.port=8082